"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.installDfx = installDfx;
const shelljs_1 = __importDefault(require("shelljs"));
const readlineSync = __importStar(require("readline-sync"));
function installDfx() {
    const request = readlineSync.question('QuikDB runs on dfx VM. Do you want to proceed with the installation? (yes/no): ');
    if (request.toLowerCase() === 'yes' || request.toLowerCase() === 'y') {
        const command = 'sh -ci "$(curl -fsSL https://internetcomputer.org/install.sh)"';
        const execOptions = {
            env: Object.assign(Object.assign({}, process.env), { DFXVM_INIT_YES: '1' }),
        };
        if (shelljs_1.default.exec(command, execOptions).code !== 0) {
            console.error('Error installing onchain tool.');
            shelljs_1.default.exit(1);
        }
        else {
            console.log('onchain tool installed successfully.');
        }
    }
    else {
        console.log('Installation aborted by the user.');
        process.exit(0);
    }
}
